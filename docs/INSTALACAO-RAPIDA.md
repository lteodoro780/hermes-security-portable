# Instalação rápida

1. Rode `scripts\windows\00-verificar-ambiente.bat`.
2. Coloque o `llama-server.exe` em `tools\llama.cpp\`.
3. Coloque o modelo em `models\model.gguf`.
4. Rode `scripts\windows\01-iniciar-servidor-ia.bat`.
5. Em outro terminal, rode `scripts\windows\02-iniciar-hermes.bat`.
